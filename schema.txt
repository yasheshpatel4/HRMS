1. Role
	roleId (PK)
	roleName 

2. User
	userId (PK)
	name
	email
	password
	dob
	doj 
	managerId
	department
	designation
	InterestedInGames

3. Role_User
	userId(FK)
	roleId(FK)


4. Travel
	travelID(PK)
	title
	description
	startDate
	endDate
	createdBy (userId )
	createdAt

5. Travel_User
	travelId (FK)
	userId (FK)

6. Notification
	notificationId(PK)
	userId (FK)
	message
	type
	Read(Boolean)
	createdAt

7. Travel_Document
	docID (PK)
	travelId (FK)
	userId (FK )
	uploadedBy
	docType
	filepath
	createdAt

8. Expense
	expenseId (PK)
	travelId (FK)
	userId (FK)
	amount
	category 
	description
	status
	hrRemarks
	submittedAt

9. Expense_Proof
	proofId (PK)
	expenseId (FK)
	Proof(doc)

10. Post
	postId (PK)
	ownerId (userID)
	title
	post(imagepath)
	createdAt

11. Likes
	postId (FK)
	userId (FK)

12. Comments
	commentId (PK)
	postId (FK)
	authorId (FK)
	text
	createdAt

13. Game
	gameId (PK)
	gameName 
	maxPlayers
	startTime
	EndTime
	slotDuration
	minPlayers
	maxplayers


14. Slots
	slotId (PK)
	gameId (FK)
	bookedBy (userId)
	startTime
	endTime
	date
	playerList....

15. Job
	jobId (PK)
	title
	summary
	jdFilePath
	hrEmail
	reviewerEmail

16. Referral
	referralId (PK)
	jobId (FK)
	referrerId(userId)
	friendName
	friendEmail
	cvfilepath
	status
	createdAt

17. Share
	shareId (PK)
	jobId (FK)
	sharedBy (userId)
	recipientEmail
	sharedAt


Relationship between tables:

User and Role: Many-to-Many   (User_Role)
User(Manager) and User: One-to-Many

User and Travel: One-to-Many
Travel and User: Many-to-Many  (travel_User)
Travel and travel_Document: One-to-Many 
User and travel_Document: One-to-Many 
Travel and Expense: One-to-Many
User and Expense: One-to-Many
Expense and Expense_Proof: One-to-Many

User and Post: One-to-Many
Post and Likes: One-to-Many
Post and Comments: One-to-Many 
User and Comments: One-to-Many 

Game and Slots: One-to-Many 
User and Slots: One-to-Many

User and Notification: One-to-Many

Job and Referral: One-to-Many
User and Referral: One-to-Many
Job and Share: One-to-Many
User and Share: One-to-Many